package com.example.islamapplictation.ui.login

import android.util.Log
import androidx.lifecycle.ViewModel

class LoginViewModel : ViewModel() {
    private  val TAG = "LoginViewModel"
    init {
        Log.d(TAG, " loginViewModel Ready ")
    }
}